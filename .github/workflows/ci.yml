# Purpose: Run the frontend test suite (engine replay fixtures) on every push
# so engine regressions surface before a deploy, not after.
name: tests

on:
  push:
  pull_request:

jobs:
  frontend-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - name: Verify uptime retry behavior
        working-directory: .
        run: python3 tools/tests/test_uptime_probe.py
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: corepack enable
      # --store-dir overrides .npmrc's /pnpm/store, which only exists in the
      # dev container image.
      - run: pnpm install --frozen-lockfile --store-dir ~/.pnpm-store
      - run: pnpm test
