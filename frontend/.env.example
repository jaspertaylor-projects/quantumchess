# --- Ads (AdSense H5 Games / Ad Placement API) ---
# Dormant until set. After AdSense approval, put your publisher id in
# .env.production (or .env.local for a staging test) and rebuild:
# VITE_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
# Force Google test ads while verifying the integration (never in production):
# VITE_ADSENSE_TEST=1
